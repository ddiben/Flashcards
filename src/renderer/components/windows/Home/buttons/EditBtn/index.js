import React from 'react';

const EditBtn = (props) => {
    
    return (
        <div className="deck-btn edit" onClick={props.onClick}>
            E
        </div>
    );

}

export default EditBtn;